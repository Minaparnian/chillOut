

<div class="container">
  <div class="jumbotron">
    <div class="container-fluid">
      <% if @current_user.present? %>
        <h1> Hi There, <%= @current_user.name %></h1>
        <h3>Create a playlist with your favorite songs</h3>
        <h3>Search for an artist to get their top 10 tracks</h3>
        <h3>Adjust the sliders to get the perfect song for you!</h3>
        <h3>Preview songs with the built in web player!</h3>
      <% end %>
    </div>
  </div>
</div>
