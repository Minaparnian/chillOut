<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to root_path do %>
            <li><%= image_tag ("Chillout2.png") %></li>
          <% end %>

       </div>
<div id="navbar" class="collapse navbar-collapse">
   <ul class="nav navbar-nav">

      <% if @current_user.present? %>

        <li><%= link_to "Search", artist_index_path(@current_user)%></li>
        <li><%= link_to "Your Playlists", artist_new_path(@current_user)%></li>
        <li><%= link_to "Sign out #{ @current_user.name }", login_path, :method => 'delete' %></li>

      <% else %>
        <li><%= link_to 'Sign in', login_path%></li>
      <% end %>
    </ul>
  </div>
 </div>
</nav>




<div class= "container">
  <div class="jumbotron">
  <% if @current_user.present? %>
    <h1> Hi <%= @current_user.name %></h1>
    <fieldset class="form-group">
      <%= text_field_tag :name, nil, :placeholder => "Enter an Artist Name", :required => 'true', :class => "form-control" %>
    </fieldset>
    <fieldset class="form-group">
      <%= submit_tag "Find Artist", :class => "button", :class => "btn btn-default"  %>
    </fieldset>
  <% end %>
  </div>
</div>
